PORT=5000
JWT_SECRET=replace-with-a-long-random-secret

# Razorpay Checkout and server-side order/signature verification
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# SMTP is required for real confirmation email delivery.
# Without these values development uses Nodemailer's JSON transport and still logs the email.
MAIL_FROM=RANNITI 5 <no-reply@ranniti5.bnikutch.com>
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=